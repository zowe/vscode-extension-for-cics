name: VSIX
on:
  push:
    branches: [ main ]
  
  workflow_dispatch:

jobs:
  build_vsix:
    name: build_vsix
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - uses: lannonbr/vsce-action@master
        with:
          args: "package"